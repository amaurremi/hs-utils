QUESTIONS

1) when i write in the terminal:
        let fileName = takeWhile (/= ',') . snd . splitAt 4
        :t (=<<) fileName
   why does it say
        (=<<) fileName :: [String] -> String
   instead of
        (=<<) fileName :: (Monad m) => m String -> String
   ?
        
                * (=<<) :: (Monad m) => (a -> m b) -> m a -> m b


